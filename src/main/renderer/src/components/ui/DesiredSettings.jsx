import React from "react";

const DesiredSettings = () => {
  return (
    <div>
      <h2 className="border border-border p-3">Desired Settings Loading</h2>
    </div>
  );
};

export default DesiredSettings;
