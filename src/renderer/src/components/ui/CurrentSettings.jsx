import React from "react";

const CurrentSettings = () => {
  return (
    <div>
      <h2 className="border border-border p-3">Current Settings Loading</h2>
    </div>
  );
};

export default CurrentSettings;
